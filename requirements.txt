# ─────────────────────────────────────────────────────────────
#  Traffic Detection System – Vercel-compatible requirements
#  (PyTorch / ultralytics / torchvision intentionally excluded
#   because they exceed Vercel's 500 MB Lambda storage limit.
#   YOLOv8 sign detection runs locally only.
#   On Vercel: HSV-based traffic-light & sign detection is used.)
# ─────────────────────────────────────────────────────────────

# Image processing (headless = no GUI, much smaller binary)
opencv-python-headless>=4.5.0
Pillow>=8.0.0
numpy>=1.19.0
scikit-image>=0.19.0

# Web framework
Flask>=3.0.0
Werkzeug>=3.0.0

# Utilities
python-dotenv>=1.0.0
